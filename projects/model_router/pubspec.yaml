name: mobileclaw_model_router
description: MobileClaw multi-provider AI model router with failover.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  mobileclaw_core:
    path: ../core
  http: ^1.2.0
  retry: ^3.1.0
  json_annotation: ^4.9.0
  meta: ^1.15.0

dev_dependencies:
  json_serializable: ^6.8.0
  build_runner: ^2.4.0
  test: ^1.25.0
