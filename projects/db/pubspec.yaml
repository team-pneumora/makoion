name: mobileclaw_db
description: MobileClaw SQLite schema, migrations, and repositories.
version: 0.0.1
publish_to: none

environment:
  sdk: ^3.6.0

resolution: workspace

dependencies:
  mobileclaw_core:
    path: ../core
  drift: ^2.22.0
  sqlite3: ^2.4.0
  json_annotation: ^4.9.0
  meta: ^1.15.0

dev_dependencies:
  drift_dev: ^2.22.0
  build_runner: ^2.4.0
  test: ^1.25.0
